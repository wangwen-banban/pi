# Sessions and runtime data (too large, no migration value)
sessions/
subagent-runs/

# Cache (auto-rebuilt)
cache/

# Local credentials (OAuth access/refresh tokens and API keys)
auth.json

# Backup files
*.bak-*

# OS files
.DS_Store
